package Revamp.renderWindow.renderItem.renderContainer;

import Revamp.renderWindow.renderItem.RenderObject;
import Revamp.renderWindow.renderItem.RenderObjectModifiers;
import Revamp.renderWindow.renderItem.RenderObjectType;
import Revamp.renderWindow.renderItem.Images.Image;

public class RenderContainer extends RenderObject{

    public RenderContainer(int x, int y, int w, int h, RenderObjectType type, Image im,
            RenderObjectModifiers[] modifs) {
        super(x, y, w, h, type, im, modifs);
        //TODO Auto-generated constructor stub
    }
    
}
 